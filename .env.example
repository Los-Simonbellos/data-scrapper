# Django
DJANGO_SECRET_KEY=llgTknThrpawtalimIvy1WaajLWUnRoV8SRQlTv6yjHTCVj1oJ0SB4Vm6VAMB-fcUA0
DJANGO_DEBUG=true

# Comma-separated list of API keys that callers must send in the
# `X-API-Key` header (or `Authorization: Bearer <key>`).
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
SERVICE_API_KEYS=KEW9FmQ3ilRaXu6xBpDB1xUE0kSV9_YHJi3w9F2MunU
# Require a valid key for every request. When true and no keys are set, the
# endpoint fails closed (all requests 401) instead of staying open.
SERVICE_REQUIRE_API_KEY=false

# Supabase (the public/anon credentials baked into the source site).
# These are read-only via the anon role + RLS, so they are safe to ship,
# but keeping them in env makes them easy to rotate if the site changes.
SUPABASE_URL=https://ghswopasaynslycpaldj.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imdoc3dvcGFzYXluc2x5Y3BhbGRqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI0MDU0MDcsImV4cCI6MjA5Nzk4MTQwN30.MifuBb6C54KQdhuv_4gMoNAGJl997ycU299OcFeoyzU
SUPABASE_TABLE=pacientes
